# Streamlit Secrets Configuration
# This file shows the format for secrets.toml (for local development)
# For Streamlit Cloud, add these secrets in the App Settings > Secrets section

# OpenAI API Key (Required)
OPENAI_API_KEY = "sk-your-api-key-here"

# Optional: Override default model
# DEFAULT_MODEL = "gpt-4o"

# Instructions for Streamlit Cloud:
# 1. Go to your app on share.streamlit.io
# 2. Click the three dots menu (⋮) > Settings
# 3. Click "Secrets" in the left sidebar
# 4. Paste your secrets in TOML format:
#
#    OPENAI_API_KEY = "sk-your-actual-key-here"
#
# 5. Click "Save"
# 6. Your app will automatically restart with the new secrets

# Instructions for Local Development:
# 1. Copy this file to .streamlit/secrets.toml
# 2. Replace "sk-your-api-key-here" with your actual API key
# 3. The secrets.toml file is automatically ignored by git
# 4. Run: streamlit run app.py

